{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "pyopencode (yaml)",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/scripts/launch_pyopencode.py",
      "args": [
        "run",
        "--provider", "deepseek",
        "--prompt",
        "编写N皇后问题解决方案的python代码并编写中文readme，测试并运行它。",
        "--cwd",
        "test",
        "--yes"
      ],
      "env": {
        "PYTHONPATH": "${workspaceFolder}/src"
      },
      "console": "integratedTerminal"
    }
  ]
}
